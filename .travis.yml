---
#sudo: required

#services:
#  - docker

cache:
  - pip

language: python

python:
  - "2.7"
  - "3.5"

install:
  - pip install tox-travis

script:
  - tox

before_cache:
  - rm ~/.cache/pip/log/debug.log
