# v1_project.yml
---
examples:

  - create:
      name: example
      state: present
    name: Create project
    assertions:
      - k8sobj.kind == 'Project'
      - k8sobj.api_version == 'v1'
      - k8sobj.metadata.name == 'example'

